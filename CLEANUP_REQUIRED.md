# Cleanup Required

The following files/directories need to be deleted to clean up the project:
- `venv/` (virtual environment)
- `testdir/` (test directory)

Run the following commands to clean up:
```bash
rm -rf /home/femirins/projects/nlrename/venv/
rm -rf /home/femirins/projects/nlrename/testdir/
```